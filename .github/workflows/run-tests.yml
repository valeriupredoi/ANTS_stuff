name: ANTS Scripts Test

on:
  push:
    branches:
      - main
  # schedule:
  #   - cron: '0 0 * * *'  # nightly

# Required shell entrypoint to have properly configured bash shell
defaults:
  run:
    shell: bash -l {0}

jobs:
  linux:
    runs-on: "ubuntu-latest"
    strategy:
      matrix:
        python-version: ["3.7"]  # ["3.9", "3.10", "3.11", "3.12"]
      fail-fast: false
    name: Linux Python ${{ matrix.python-version }}
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: conda-incubator/setup-miniconda@v2
        with:
          activate-environment: ants-scripts
          environment-file: scripts/ants_environment.yml
          python-version: ${{ matrix.python-version }}
          miniforge-version: "latest"
          miniforge-variant: Mambaforge
          use-mamba: true
      - name: Gather basic env information
        run: |
          conda --version
          python -V
      - name: Install ANTS
        run: |
          cd ants
          pip install -e .
      - name: Install ANCIL scripts
        run: |
          cd scripts
          pip install -e .
      - name: Test ANCIL commands[NitrogenDeposition]
        run: |
          scripts/NitrogenDeposition/cmip6_ndep_jasmin_vn21.py --help
          scripts/NitrogenDeposition/cmip6_ndep_jasmin_vn23_ssp585.py --help
          scripts/NitrogenDeposition/cmip6_ndep_jasmin_vn25_past_and_future.py --help
          scripts/NitrogenDeposition/cmip6_ndep_jasmin_vn26_past_and_future.py  --help
      - name: Test ANCIL scripts[NitrogenDeposition]
        run: pytest scripts/NitrogenDeposition
      - name: Test ANCIL scripts[GreenhouseGases]
        run: pytest scripts/GreenhouseGases
